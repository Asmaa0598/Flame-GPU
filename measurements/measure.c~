#include "mainH2Air.h"
#include <time.h>

int main() {

    // Output files
    FILE* output;
    FILE* measurements;

    // Opening the files in write mode
    output = fopen("outputZeldovich.txt", "w");
    measurements = fopen("costZeldovich.txt", "w");

    // --------------------------------------------------------------------------------
    // number of iterations
    const int nIter = 10;
    // ranges limits
    const double minT = 300.0;
    const double maxT = 3500.0;
    const double minP = 1.0e+04;
    const double maxP = 1.0e+08;
    // creating the temperature and pressure arrays:
    double T[nIter];
    double P[nIter];
    for (int i = 0; i < nIter; i++) T[i] = (double)rand() % (maxT - minT + 1) + minT;
    for (int i = 0; i < nIter; i++) P[i] = (double)rand() % (maxP - minP + 1) + minP;
    // The mass fractions array:
    double Y[nsp] = { 0.231527, 0.495993, 0.0, 0.27248, 0.0, 0.0, 0.0 };
    // --------------------------------------------------------------------------------
    // Mechanism specific information 


    // --------------------------------------------------------------------------------
    double omegas[nsp] = {0.0};
    // Start time
    clock_t start_time = clock();
    // main loop
    // Calculate elapsed time in seconds
    double elapsed_time = (double)(end_time - start_time) / CLOCKS_PER_SEC;
    // --------------------------------------------------------------------------------
    // writing in files:
    // writing cost:
    // writing omegas:

    
  return 0; 
}
